var numero = parseInt(prompt("Digite um número inteiro para verficar a tabuada"));

var resultado = 0;

for (let i = 0; index <= 10; index++) {
    resultado += `${numero} x ${i}` = ${numero * i};   
}

console.log(resultado);

